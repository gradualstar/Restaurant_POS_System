import React from "react";

const Header = () => {
  // Returning null tells React to render absolutely nothing.
  // This removes the header from ALL pages in your app.
  return null; 
};

export default Header;